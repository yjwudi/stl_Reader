# stl_Reader
The program can read and show the object from the .stl file using opengl. The user can rotate, zoom and pick the fragment.
click left button will pick the fragment
click left button and drag the mouse can rotate
click right button will enlarge and shrink the object
